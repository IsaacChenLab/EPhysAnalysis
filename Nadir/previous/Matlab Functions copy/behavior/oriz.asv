scrsz = get(0,'ScreenSize');
f1=figure('Position',scrsz,'color',[0 0 0]);
drawnow;
m=0; %for oriz, alfa=0; tg(alfa=0)=sin0/cos0=0/1=0; so m=0; if alfa=90 (vert)  
hold on;
for n=-5:5
for i=1:5
    x1(i)=i;
    y1(i)=m*x1(i)+n;
end
plot(y1,'w');
end
%axis square;
axis fill;
axis off;